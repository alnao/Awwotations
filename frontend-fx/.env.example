# Copy to .env. The only setting here is the path of the real config file;
# everything else (API style, URLs, JWT token) lives in that file.
# If the file does not exist it is created on first run and the
# administration dialog opens automatically.
CONFIG_FILE=~/.config/jawwotations.config
